{
  "name": "backend",
  "engines": {
    "node": ">=18.18.0 <20.10.0"
  },
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npx playwright install && node app",
    "dev": "npx nodemon app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright": "^1.40.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
